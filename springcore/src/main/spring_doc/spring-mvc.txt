spring-mvc
---spring-mvc
1)a spring mvc if a sub framework of spring framework which is used
to build a web application 
2) it is build on top of serlvet api
3)it follows the model - view - controller 
4) it implements all the basic feature of core spring framework like inversion of control , dependency injection
why spring mvc
1) seperate each role modal , view , controller
2)powerful configuration 
3) it is sub  framework of spring framework
4) rapid application developement 
5) spring mvc is flexible, easy to test and much feature

---mvc design pattern
--modal-data 
--view data
--controller interface between model and view

---mvc design pattern 
way to organize the code in our application

---working of mvc
--request -> controller -> render response -> return to 

---creating webapp
--creating quick-web app in artype
--spring web mvc will download , core , context spel and other
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-webmvc</artifactId>
			<version>5.3.22</version>
		</dependency>

<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<version>3.3.1</version>
			</plugin>
		</plugins>
		<finalName>springweb</finalName>
	</build>


2)
-configure the dispatcher serlvet in web.xml
-create spring configuration file
-configure view resolver
-create controller
-create a view to show page


---need to inable annotation base @controller 
put that line in xml

